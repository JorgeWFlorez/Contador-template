---

---

<section id="about" class="about">
  <div class="container">
    <div class="about-left">
      <h2 class="fade-in-up">Confiable, Profesional y Cercana</h2>
    </div>
    <div class="about-right">
      <p class="fade-in-up">
        Con años de experiencia ofreciendo servicios contables a empresas y
        particulares, garantizamos resultados claros y confianza en cada
        proceso.
      </p>
      <div class="about-stats fade-in-up">
        <div class="stat">
          <span class="stat-green">+115</span>
          <span class="stat-text">Clientes</span>
        </div>
        <div class="stat">
          <span class="stat-green">+10</span>
          <span class="stat-text">Años de experiencia</span>
        </div>
        <div class="stat">
          <span class="stat-green">+55</span>
          <span class="stat-text">Empresas asesoradas</span>
        </div>
        <div class="stat">
          <span class="stat-green">99%</span>
          <span class="stat-text">Satisfación</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    padding-top: clamp(80px, 10vw, 140px);
    padding-left: 3vw;
    padding-right: 3vw;
    padding-bottom: 4vw;
    display: flex;
    justify-content: center;
    align-items: flex-start;
  }

  .container {
    display: flex;
    width: 100%;
    flex-wrap: wrap;
    gap: 6vw;
    justify-content: center;
    max-width: 1400px;
  }

  .about-left {
    flex: 1;
    min-width: 300px;
  }

  .about-right {
    flex: 1;
    min-width: 300px;
    display: flex;
    flex-direction: column;
  }

  .about-right p {
    margin-bottom: 30px;
  }

  /* Stats container */
  .about-stats {
    display: flex;
    flex-wrap: wrap;
    gap: 3vw;
  }

  /* Cada stat individual */
  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    min-width: 100px;
    flex: 1 1 30px;
  }

  /* Número destacado */
  .stat-green {
    color: var(--color-green);
    font-weight: var(--fw-semibold);
    font-size: var(--fs-paragraph);
  }

  /* Texto descriptivo */
  .stat-text {
    color: var(--color-black);
    font-weight: 400;
    font-size: 16px;
    margin-top: 5px;
    text-align: center;
  }
</style>
